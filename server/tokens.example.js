"use strict";

// Copy this file to tokens.local.js and fill in real tester tokens for
// local dev. tokens.local.js is gitignored, never commit real tokens.
// In production (Render, etc.), set the TESTER_TOKENS env var instead
// (comma-separated) and skip this file entirely.
module.exports.TESTER_TOKENS = [
  "nm_replace_with_a_real_token",
];
